# ═══════════════════════════════════════════════════════════════════════════
# Taskana - WhatsApp Habits & Tasks Manager
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════════════════════════

# ── WhatsApp Configuration ─────────────────────────────────────────────────
# Messaging provider (currently only 'baileys' supported)
MESSAGING_PROVIDER=baileys

# Directory to store WhatsApp session data
WHATSAPP_SESSION_DIR=./data/session

# Your phone number (only messages from this number will be processed)
# Format: country code + number, no spaces or dashes (e.g., 20xxxxxxxxxx)
MY_PHONE_NUMBER=

# ── Location (for Islamic prayer times) ────────────────────────────────────
# Latitude and longitude of your location
LATITUDE=30.7865
LONGITUDE=31.0004

# Prayer time calculation method
# Options: egyptian, mwl, isna, karachi, umm_al_qura, dubai, qatar, kuwait, singapore
PRAYER_CALC_METHOD=egyptian

# ── OpenAI Configuration ───────────────────────────────────────────────────
# Your OpenAI API key (required for Whisper STT and GPT intent classification)
OPENAI_API_KEY=

# Speech-to-text provider (currently only 'whisper-api' supported)
STT_PROVIDER=whisper-api

# Default language hint for Whisper transcription
WHISPER_DEFAULT_LANG=ar

# ── File Paths ─────────────────────────────────────────────────────────────
# Directory containing habit definition JSON files
HABITS_DIR=./data/habits

# Directory for daily log JSON files
DAYS_DIR=./data/days

# Directory for saved media (images, audio)
MEDIA_DIR=./data/media

# ── Application ────────────────────────────────────────────────────────────
# Port for the HTTP server (not used for WhatsApp, but required by NestJS)
PORT=3000

# Environment: development | production | test
NODE_ENV=development