/**
 * Button option for interactive button messages.
 */
export interface ButtonOption {
  /** Internal ID returned on tap (max 256 chars) */
  id: string;
  /** Display text (max 20 chars) */
  text: string;
}
